// Parser frontend grammar (written in the parser for the same parser)
%file "/Users/leegao/sideproject/ParserSiProMo/experimental"

%require "ll1_tokenizer"

%convert {: :}

%production hello

root := LOL $hello [a] | woow $root x y z {:
  function(woow, root, _, _, _)
    print(woow)
  end
:}
